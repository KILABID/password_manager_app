# App Configuration
APP_PORT=

# Primary / Auth Database Configuration
DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_SSLMODE=

# Dedicated Vault Database Configuration (Optional - fallback to Primary DB if empty)
VAULT_DB_HOST=
VAULT_DB_PORT=
VAULT_DB_USER=
VAULT_DB_PASSWORD=
VAULT_DB_NAME=
VAULT_DB_SSLMODE=

# Vault Encryption Key (AES-256 Secret Key)
VAULT_ENCRYPTION_KEY=

# JWT Authentication Configuration
JWT_SECRET=
JWT_EXPIRATION_HOURS=
JWT_ISSUER=